name: deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.repository != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Deploy placeholder
        run: |
          echo "Configure SSH-based GitHub-first deployment here."
          echo "Example target: pull repo on VM, recreate venv, restart service."
          echo "No direct manual agent deployment is used."
